# ===============================
# DEFAULT PROFILE
# ===============================
# Use: -Dspring.profiles.active=dev or -Dspring.profiles.active=prod
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# This file now delegates to profile-specific configurations
# See: application-dev.properties and application-prod.properties